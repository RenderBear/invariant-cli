#!/bin/sh
# Source-tree launcher for the optional local-agent harness.
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
python="$root/.venv/bin/python"
if [ ! -x "$python" ]; then
  python=${PYTHON:-python3}
fi

PYTHONPATH="$root/src${PYTHONPATH:+:$PYTHONPATH}" exec "$python" -P -m invariant.harness.cli "$@"
