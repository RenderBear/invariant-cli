# Packaged skill bundles (build artifacts)
*.skill
dist/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Local agent state — installs are symlinks, not repo content
.claude/
.cursor/
.codex/
!AGENTS.md

# Editors
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# NOTE: .invariant/ is deliberately NOT ignored. Policy, accepted records,
# architecture pointers, discoveries, and audits are reviewed repository state.
# Runtime worktree materializations and caches are disposable; initialization
# also adds this path to the repository's local info/exclude file.
.invariant/runtime/
