# Packaged skill bundles (build artifacts)
*.skill
dist/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Local agent state — installs are symlinks, not repo content
.claude/
.cursor/
.codex/
!AGENTS.md

# Editors
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# NOTE: .invariant/ is deliberately NOT ignored. Accepted domains and contracts,
# architecture pointers, discoveries, and audits are reviewed repository state.
# Every generated task artifact lives under `.invariant/runtime/`. The CLI also
# creates an ignore marker inside that directory so this holds in adopted repositories.
.invariant/runtime/
